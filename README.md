# Арифметическое кодирование на Python

## Описание

Реализация алгоритма арифметического кодирования с поддержкой кодирования и декодирования текстовых файлов. Алгоритм обеспечивает реальное сжатие данных, приближаясь к теоретическому пределу энтропии Шеннона.

## Файлы

- **arithmetic_coding.py** - основной модуль с алгоритмом
- **arithmetic_gui.py** - графический интерфейс
- **compare_algorithms.py** - сравнение с Хаффманом
- **test_arithmetic.txt** - тестовый файл

## Использование через командную строку

### Сжатие файла
```bash
python arithmetic_coding.py compress input.txt output.bin
```

### Распаковка файла
```bash
python arithmetic_coding.py decompress input.bin output.txt
```

## Использование через GUI

```bash
python arithmetic_gui.py
```

### Возможности GUI:
- ✍️ Ввод текста для кодирования
- 📂 Загрузка текста из файла
- ⚙️ Кодирование с визуализацией результата
- 📊 Таблица частот и кумулятивных вероятностей
- 🔢 Просмотр закодированных битов
- 📈 Статистика: энтропия, эффективность, степень сжатия
- 📦 Сжатие/распаковка файлов

## Сравнение алгоритмов

Для сравнения Хаффмана и арифметического кодирования:

```bash
python compare_algorithms.py test_arithmetic.txt
```

## Технические детали

### Алгоритм
- Использует целочисленную арифметику (32 бита)
- Масштабирование для предотвращения переполнения
- E1, E2, E3 масштабирование
- Сохранение таблиц частот для декодирования

### Структура сжатого файла
```
[4 байта] - длина исходного текста
[4 байта] - количество закодированных битов
[1 байт]  - padding битов
[4 байта] - размер таблицы частот
[N байт]  - таблица частот (pickle)
[4 байта] - размер кумулятивных частот
[M байт]  - кумулятивные частоты (pickle)
[4 байта] - общая частота
[K байт]  - закодированные данные
```

## Результаты тестирования

На тестовом файле (1131 символ):

| Алгоритм | Размер | Сжатие | Эффективность |
|----------|--------|--------|---------------|
| Оригинал | 9048 бит | - | - |
| Теория | 5312 бит | 41.30% | 100.0% |
| Хаффман | 5352 бит | 40.85% | 99.2% |
| **Арифметическое** | **5324 бит** | **41.16%** | **99.8%** |

✓ Арифметическое кодирование показывает лучший результат

## Преимущества

✅ **Оптимальное сжатие** - приближается к энтропии
✅ **Универсальность** - работает с любыми символами
✅ **Эффективность** - лучше Хаффмана на 0.5-3%
✅ **Детерминированность** - одинаковый результат при каждом запуске
✅ **Без потерь** - точное восстановление данных

## Особенности реализации

### Масштабирование
Для предотвращения переполнения используется три типа масштабирования:
- **E1**: high < HALF → сдвиг влево, вывод 0
- **E2**: low >= HALF → сдвиг влево, вывод 1  
- **E3**: QUARTER <= low < HALF <= high < 3*QUARTER → сдвиг с pending битами

### Точность
- 32-битная целочисленная арифметика
- Нет потерь точности при вычислениях
- Корректная работа с интервалами любого размера

## Примеры

### Пример 1: Простой текст
```python
from arithmetic_coding import ArithmeticCoder

coder = ArithmeticCoder()
text = "hello world"
bits = coder.encode(text)
decoded = coder.decode(bits, len(text))
print(''.join(decoded))  # "hello world"
```

### Пример 2: Работа с файлами
```python
from arithmetic_coding import compress_file, decompress_file

compress_file("input.txt", "compressed.bin")
decompress_file("compressed.bin", "output.txt")
```

## Ограничения

- Работает только с текстовыми файлами (UTF-8)
- Требует сохранения таблицы частот (overhead)
- Медленнее, чем Хаффман (~2x)
- Не адаптивное (таблица строится один раз)

## Дальнейшие улучшения

Возможные направления развития:
- Адаптивное кодирование (обновление частот на лету)
- Поддержка бинарных файлов
- Контекстное моделирование
- Оптимизация скорости
- Параллельная обработка

## Требования

- Python 3.6+
- Стандартная библиотека (pickle, tkinter для GUI)

## Лицензия

Код предоставляется для образовательных целей.